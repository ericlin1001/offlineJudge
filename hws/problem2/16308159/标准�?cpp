#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	float a,b,c,d,e,f,g;
	cin>>a>>b>>c>>d;
	e=(a+b+c+d)/4;
	f=pow(a-e,2)+pow(b-e,2)+pow(c-e,2)+pow(d-e,2);
	g=sqrt(f/4);
	cout<<g<<endl;
	return 0;
}
