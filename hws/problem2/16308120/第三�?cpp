#include<iostream>
#include<cmath>
using namespace std;
int main()
{
int a,b,c,d;
float e,f,g;
cin>>a>>b>>c>>d;
e=(a+b+c+d)/4.0;
f=pow((a-e),2)+pow((b-e),2)+pow((c-e),2)+pow((d-e),2);
f=f/4;
g=sqrt(f);
cout<<e<<'\t'<<g<<endl;
}
